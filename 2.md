## 🤖 Assistant

问得对——文件谁写、放哪、怎么运转，这一步搞不清组里会乱。下面把整个项目的**物理部署和运转方式**讲透。

## 一、核心概念：两个角色，一套仓库

整个项目里有三个身份：

| 身份 | 对应人 | 做的事 |
|------|-------|--------|
| **攻击者** | B | 写投毒工具、恶意内容、接收窃取的数据 |
| **受害者** | A | 拥有 Agent 平台，会"安装"B 写的第三方工具 |
| **实验员** | 三人共同 | 用判定器和管线测量攻击成功率（这个身份独立于前两个） |

**物理上所有代码都放在一个 Git 私有仓库里，三个人各自 clone 到自己电脑。** 文件"在谁的电脑"有两层含义：谁**写**它、它**运行**在哪——分开看。

## 二、文件归属与运行位置总表

| 文件 | 谁写 | 运行时在哪里 | 作用 | 什么时候用 |
|------|------|-------------|------|-----------|
| `agent.py` `tools.py` `config.py` `web_content.py` `run.py` | A | 受害者电脑 | 受害者的 Agent 平台 + 自己的工具（文件、邮件） | 全程 |
| `mcp_client.py` | A | 受害者电脑 | 受害者"安装第三方工具"的连接入口 | 阶段一之后 |
| `mcp_server_clean.py` | B | 受害者电脑（作为子进程） | 干净的对照工具 | 联调、对照组 |
| `mcp_server_news.py` | B | **受害者电脑**（作为子进程） | 投毒工具——受害者"下载安装"的东西 | 攻击 A 演示与实验 |
| `receiver.py` | B | **B 自己的电脑** | 攻击者接收端，收取外泄数据 | 仅攻击 A' 演示 |
| `samples/*.json` | B | 跑实验的机器 | 攻击语料库（冻结后不许改） | 攻击+防御阶段 |
| `experiments/loader.py` `attack_*.py` | B | 跑实验的机器 | 布置攻击环境、内存注入 | 实验时 |
| `experiments/judge.py` `run_experiment.py` | A+B 共同 | 跑实验的机器 | 判定攻击是否成功、出 CSV | 实验时 |
| `defense/*.py` | C | 受害者电脑 | 三道防线 | 防御阶段 |
| `results/*.csv` | 自动生成 | 仓库里共享 | 实验数据 | 全程积累 |

## 三、关键原理：为什么 B 写的文件会"跑"在 A 的电脑上

这是整个项目最需要理解的一点，也是它**模拟现实**的地方：

```
B 写 mcp_server_news.py（投毒工具）
        │  push 到 Git 仓库
        ▼
A 在受害者电脑上 git pull（剧情里 = 用户从网上下载了第三方工具）
        │
        ▼
受害者的 mcp_client.py 把它作为子进程启动（stdio 模式）
        │
        ▼
攻击者的代码就运行在受害者机器上了
```

**MCP stdio 模式下，第三方 server 代码本来就运行在安装者的机器上**——这跟浏览器插件一样：插件作者是别人，但代码在你的电脑上跑。所以：

- **攻击 A/B/C 运行时，B 不需要在场**。投毒代码已经在 A 的电脑上，攻击全程发生在受害者机器内部。
- **只有攻击 A'（恶意实现 + 数据外泄）需要 B 的电脑在线**：受害者的工具通过 HTTP 把数据 POST 到 B 的接收端。

Git 仓库在这里有双重身份：既是团队的协作工具，**又是剧情里的"供应链"**——A pull 代码就是受害者"下载工具"，这个对应关系可以写进报告。

## 四、两种物理部署方案

### 方案一：单机（日常开发实验用，推荐）

所有东西在一台电脑上（建议 A 的，因为 Agent 平台和模型在 A 手里），靠文件夹和进程区分角色：

```bash
# 日常实验全在 A 的电脑上跑：
python experiments/run_experiment.py --suite attack_a --runs 3
```

B 和 C 在自己电脑上写代码 → push → A pull → 在 A 机上跑实验。**90% 的工作量都是这个模式。**

### 方案二：双机（答辩演示用）

```
┌─ A 的笔记本（受害者）──────────────┐      ┌─ B 的笔记本（攻击者）────┐
│ agent.py + tools.py（自有工具）    │      │ receiver.py 监听 8000    │
│ mcp_client.py（安装入口）          │      │                          │
│ mcp_server_news.py（已装的毒工具） │      │   ▲                      │
│ defense/（答辩时开启）             │──────┼───┘ HTTP POST 外泄数据   │
└───────────────────────────────────┘      └──────────────────────────┘
         两台电脑连同一个 WiFi（手机热点即可）
```

演示时攻击 A/B/C 在 A 机上发生，A' 攻击时 B 的屏幕**实时打印收到的数据**——这是全场最震撼的环节。

## 五、协作流程（具体操作）

### 1. 建仓库（第一天就做）

```bash
# 一人（建议 A）创建私有仓库 agent-security-lab，三人 clone
git clone <仓库地址>
# 注意：.gitignore 里加 .venv/ logs/ 和 API key 配置
# API key 绝不能进仓库：config.py 里 key 从环境变量或本地 gitignored 文件读
```

### 2. 日常循环

```
写代码（各自电脑）→ 本地自测 → git push → 队友 pull → 在实验机上跑
```

### 3. 冻结语料（阶段六的关键操作）

```bash
# 攻击数据定稿后立即打标签，之后谁都不许动 samples/
git add samples/ && git commit -m "freeze attack corpus"
git tag freeze-attack-v1
```

防御阶段如果 ASR"莫名变好"，第一件事就是 `git diff freeze-attack-v1 -- samples/` 检查语料有没有被偷改。

### 4. 一次完整实验的运转过程（以攻击 A 为例）

```
1. A 在受害者机 pull 最新代码
2. 确认 mcp_client.py 里 SERVER_CMD 指向 mcp_server_news.py（投毒版）
3. 执行: python experiments/run_experiment.py --suite attack_a --runs 3
4. 管线自动做:
   loader 读 attack_a.json → 内存注入载荷到目标工具 description
   → 起 Agent（连接投毒 server）→ 跑对话 → 写 logs/*.json
   → judge.py 判定每轮是否成功 → 追加 results/attack_a.csv
5. A push results/，三人都能看到 ASR
```

## 六、答辩演示走查（双机版，5 分钟）

| 时间 | A 的操作（受害者机） | B 的操作（攻击者机） | 观众看到 |
|------|---------------------|---------------------|---------|
| 0:00 | 演示正常任务 | — | Agent 正常干活 |
| 1:00 | `mcp_client.py` 切换指向投毒 server，说"现在安装一个第三方新闻工具" | — | 安装动作就是改配置，剧情点题 |
| 2:00 | 提问"查今天的新闻" | — | 日志显示 Agent 偷偷调用 send_email 发往 attacker@evil.com |
| 3:00 | 提问新闻（A' 版工具） | receiver.py 早已运行 | **B 的屏幕实时弹出受害者数据** |
| 4:00 | 开启 defense 模块，重复同样攻击 | — | 攻击被 D2/D3 拦截 |
| 4:30 | 展示 ASR 三张图 | — | 收尾 |

## 七、回答你最初那个困惑

"组员在他电脑上写的 attack.py 如何注入到我的电脑？"

**答案：不需要注入。** B 写的投毒工具通过 Git 仓库流转到你的电脑，剧情上就是"受害者下载安装了攻击者发布的工具"。工具 description 里的恶意指令是它**出厂自带**的，不是事后篡改的。攻击者控制 description 的方式是"他写了一个带毒的 description 并让你装上它"，而不是"黑进你的电脑改代码"。

所以项目里根本不存在"攻击者入侵受害者电脑"这一步——你们模拟的是**供应链攻击**：用户主动安装了坏工具，攻击随之生效。这个理解到位了，答辩时任何"这跟现实有什么关系"的问题都难不住你们。
